# Project Guidance

This repository implements `k10fetcher`, a local CLI MVP for fetching SEC 10-K filings.

## ExecPlans

When implementing complex features or significant refactors, use an ExecPlan as described in `.agent/PLANS.md`. The active MVP execution plan is `.agent/execplans/k10fetcher_mvp_execplan.md`.

Keep ExecPlans self-contained and update `Progress`, `Surprises & Discoveries`, `Decision Log`, and `Outcomes & Retrospective` as work proceeds.